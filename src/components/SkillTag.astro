---
import type { Skill } from "../interfaces/skill";

interface Props {
  skill: Skill;
}

const { skill } = Astro.props;
---

<div
  class:list={[
    "flat-border-with-shadow rounded-full px-4 py-1.5 md:px-6 md:py-3",
    { "font-semibold": skill.important },
  ]}
>
  {skill.name}
</div>
